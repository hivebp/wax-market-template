import React from 'react'

/**
 * @type {import('next').NextPage<{ tab?: string }>}
 */
export const PrivacyPage = (props) => {
    return <div>We believe in privacy</div>
}

export default PrivacyPage
